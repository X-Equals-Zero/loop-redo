// import { writable } from "svelte/store";

import { writable } from "svelte/store";

// export const dimensionsHandle = writable({
//     x: 0,
//     y: 0,
// });
export const canvasDims = writable({x: 0, y: 0});